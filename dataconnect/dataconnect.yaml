specVersion: "v1"
serviceId: "review-pulse-portal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "review-pulse-portal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
